Place static files for `verify_metrics` docs, such as images and sample data, in this directory.

They can be referenced in reStructuredText using absolute paths, e.g.:

```
.. image:: /_static/verify_metrics/image.jpg
```
